<h1>Editing Book</h1>
<%= form_with model: @book do |book| %>

<h4>Title</h4>
  <%= book.text_field :title %>
  
  <h4>body</h4>
  <%= book.text_area :body %>

<%= book.submit 'Update Book' %>

<% end %>